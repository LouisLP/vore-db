/**
 * Drizzle schema for vore-db.
 *
 * Empty on purpose: the stack was proven end to end with a throwaway `smoke_test`
 * table (generate → migrate → insert → select → drop), which was then removed.
 * The real schema lands here — see issue #8.
 */

export {}
